

const Header = (props) => {
    return (
        <div>
            <h1 className="text-center text-lg font-extrabold  text-red-700 ">{props.tittle}</h1>
        </div>
    );
};

export default Header;